// import { ConsoleReporter } from "jasmine";

import { parseMolecule } from './codewars';

describe("codewars", function() {

  // beforeEach(function() {});

  it("should be", function() {

//     const chunk = `
// `.trim() + '\n';
//     const expected: string = `
// `.trim();
    // expect(4).toEqual(6);

    let s = G964.stockList(b, c)
    expect(s).toEqual(res);

  });
});
